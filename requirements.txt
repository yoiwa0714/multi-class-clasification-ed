# 純正ED法（Error Diffusion Learning Algorithm）実装 v0.2.0
# 必要なPythonパッケージ
# Installation: pip install -r requirements.txt

# 必須依存関係（Core Dependencies）
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.62.0

# データセット・機械学習依存関係（Dataset & ML Dependencies）
torch>=1.10.0
torchvision>=0.11.0

# GPU高速化依存関係（GPU Acceleration - Optional）
# CuPyはGPU環境でのみ必要。CPU環境では自動的にスキップされます
# cupy-cuda11x>=10.0.0  # CUDA 11.x環境用（コメントアウト状態）
# cupy-cuda12x>=12.0.0  # CUDA 12.x環境用（コメントアウト状態）

# 開発・テスト依存関係（Development Dependencies - Optional）
# pytest>=6.0.0
# black>=21.0.0
# flake8>=3.9.0

# =============================================================================
# インストール手順（Installation Guide）
# =============================================================================
#
# 1. 基本環境（CPU環境）:
#    pip install numpy matplotlib tqdm torch torchvision
#
# 2. GPU環境（CUDA対応）:
#    a) CUDA 11.x環境:
#       pip install numpy matplotlib tqdm torch torchvision cupy-cuda11x
#    b) CUDA 12.x環境:
#       pip install numpy matplotlib tqdm torch torchvision cupy-cuda12x
#
# 3. 全依存関係（requirements.txtを使用）:
#    pip install -r requirements.txt
#
# 注意事項：
# - CuPyはオプション依存関係です。インストールされていない場合は自動的にCPU実行に切り替わります
# - CUDA環境に応じて適切なCuPyバージョンを選択してください
# - Torchvisionは最新の安定版を推奨します
#
# =============================================================================
# バージョン互換性情報（Version Compatibility）
# =============================================================================
#
# Python: 3.8以上推奨（3.8+）
# OS: Linux, Windows, macOS対応
# GPU: NVIDIA GPU（CUDA対応）でのみGPU高速化が利用可能
#
# テスト済み環境：
# - Python 3.8-3.11
# - NumPy 1.21-1.24
# - PyTorch 1.10-2.0
# - CUDA 11.x, 12.x
#
